apiVersion: v1
kind: Service
metadata:
  name: seller-console
  namespace: ui
  labels:
    app: seller-console
    component: seller-console
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: seller-console

