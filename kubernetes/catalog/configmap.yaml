apiVersion: v1
kind: ConfigMap
metadata:
  name: catalog-service-config
  namespace: catalog
data:
  KAFKA_BOOTSTRAP_SERVERS: "kafka-cluster-kafka-bootstrap.kafka.svc.cluster.local:9092"
  KAFKA_EVENTS_TOPIC: "catalog.events"
  KAFKA_INVENTORY_EVENTS_TOPIC: "inventory.events"
  INVENTORY_SERVICE_URL: "http://inventory-service.inventory.svc.cluster.local:8083"

