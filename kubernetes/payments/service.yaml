apiVersion: v1
kind: Service
metadata:
  name: payments-service
  namespace: payments-platform
  labels:
    app: payments-service
    component: payments
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: payments-service

