apiVersion: v1
kind: ConfigMap
metadata:
  name: frontend-config
  namespace: payments-platform
data:
  # VITE_API_BASE_URL is no longer needed - frontend uses /api which nginx proxies
  # VITE_STRIPE_PUBLISHABLE_KEY is set at build time via Dockerfile ARG
  # This ConfigMap is kept for future runtime configuration if needed

