apiVersion: v1
kind: ConfigMap
metadata:
  name: admin-console-config
  namespace: payments-platform
data:
  # VITE_API_BASE_URL is no longer needed - admin-console uses /api which nginx proxies
  # This ConfigMap is kept for future runtime configuration if needed

