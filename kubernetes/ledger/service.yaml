apiVersion: v1
kind: Service
metadata:
  name: ledger-service
  namespace: payments-platform
  labels:
    app: ledger-service
    component: ledger
spec:
  type: ClusterIP
  ports:
  - port: 8081
    targetPort: 8081
    protocol: TCP
    name: http
  selector:
    app: ledger-service

